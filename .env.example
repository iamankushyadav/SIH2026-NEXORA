PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/pathfinder
JWT_SECRET=replace-with-a-long-random-secret
# Optional: only used to seed local demo accounts when running server.js directly.
DEMO_PASSWORD=replace-with-a-demo-password
